#!/bin/sh
export PYTHONPATH="@BLOOM_SOURCE_DIR@/src${PYTHONPATH:+:$PYTHONPATH}"
export BLOOM_CATALOG="${BLOOM_CATALOG:-@BLOOM_BINARY_DIR@/catalog.json}"
export BLOOM_VBUF_LIBRARY="${BLOOM_VBUF_LIBRARY:-@VBUF_CORE_LIBRARY@}"
exec "@Python3_EXECUTABLE@" -m bloom "$@"
